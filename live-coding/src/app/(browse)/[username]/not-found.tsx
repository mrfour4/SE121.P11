import { PageNotFound } from "@/components/page-not-found";

export default function NotFoundPage() {
    return (
        <PageNotFound message="We couldn't find the user you were looking for." />
    );
}
